[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build --workspace @trayloop/api"]

[start]
cmd = "npm run start --workspace @trayloop/api"
